// import 

export default function Categories({ allCategories }) {
// const [activeCategory, setActiveCatecory] = useState(null)

  return (
<div>
{/* //   <h1>Categories</h1>
//   {allCategories.map((cat, idx) => 
//   <button onClick={() => setActiveCategory(cat.name)}>{cat.name}</button>)}
//   {<CategoyItems allIte/>} */}
</div>
    )
}